# environment configuration
settings:
  docker:
    requirements: requirements.txt

# pipeline configuration
parameters:
  dataset_id: zenml-codegen-v1

steps:
  mirror_repositories:
    parameters:
      repositories:
        - zenml